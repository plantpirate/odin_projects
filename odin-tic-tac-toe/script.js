const ticTacToe = (function () {
  let gameboard = [];

  const createPlayer = (name) => {
    const playerName = name;

    let score = 0;
  };
})();

function GameBoard() {
  const rows = 3;
  const columns = 3;
  const board = [];
}
